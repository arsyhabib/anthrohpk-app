# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# Virtual environment
.venv/

# Environment variables and secrets
.env

# OS generated files
.DS_Store
Thumbs.db

# Keystore files (used for Android signing)
*.keystore
*.jks
# Outputs
outputs/*.pdf
outputs/*.csv

# Logs
*.log

# Static files (auto-generated)
static/sticker_*.png
```

5. Scroll ke bawah, tulis commit message: `Update .gitignore`
6. Klik **Commit changes**

---

## ✅ **STEP 5: Verifikasi File di GitHub**

Pastikan repository Anda sekarang memiliki file-file ini:
```
📁 gizisikecil/
├── 📄 .gitignore
├── 📄 README.md
├── 📄 app.py           ✅ (3500+ baris)
└── 📄 requirements.txt ✅
```

**Cara cek:**
- Buka `https://github.com/YOUR_USERNAME/gizisikecil`
- Pastikan ada file `app.py` dan `requirements.txt`
- Klik `app.py` untuk preview - pastikan kode lengkap terlihat

---

## 🚀 **STEP 6: Deploy ke Render.com**

### 6.1. Login ke Render

1. Buka **https://render.com**
2. Klik **Get Started** atau **Sign In**
3. **Pilih "Sign in with GitHub"** (PENTING!)
4. Authorize Render untuk akses GitHub

### 6.2. Create New Web Service

1. Di Render Dashboard, klik **New +** (biru)
2. Pilih **Web Service**
3. Di halaman "Create a new Web Service":
   - Jika repository Anda tidak muncul, klik **+ Connect account** atau **Configure account**
   - Authorize Render untuk akses repository Anda
   - Cari repository: `gizisikecil`
   - Klik **Connect**

### 6.3. Configure Service

**Isi form berikut:**

| Field | Value |
|-------|-------|
| **Name** | `gizisikecil-app` (atau nama lain, harus unik) |
| **Region** | Singapore (Asia) |
| **Branch** | `main` |
| **Root Directory** | (kosongkan) |
| **Runtime** | Python 3 |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `uvicorn app:app --host 0.0.0.0 --port $PORT` |

**Scroll ke bawah:**

| Field | Value |
|-------|-------|
| **Instance Type** | Free |

### 6.4. Environment Variables (Opsional - bisa skip)

Klik **Advanced** jika ingin tambah environment variables:

- Klik **Add Environment Variable**
- **Key:** `PYTHON_VERSION` → **Value:** `3.11.0`
- Klik **Add Environment Variable** lagi
- **Key:** `HOST` → **Value:** `0.0.0.0`

### 6.5. Deploy!

1. Scroll ke paling bawah
2. Klik **Create Web Service** (tombol biru besar)
3. Tunggu proses build (5-15 menit pertama kali)

**Monitor progress:**
- Tab **Logs** akan menampilkan proses instalasi
- Tunggu sampai muncul: `✅ Application startup complete`

---

## 🎉 **STEP 7: Aplikasi LIVE!**

### 7.1. Dapatkan URL

Setelah deploy selesai (status: **Live**), URL aplikasi Anda:
```
https://gizisikecil-app.onrender.com
```

*(Sesuaikan dengan nama service yang Anda buat)*

### 7.2. Test Aplikasi

Buka di browser:

1. **Main UI:** `https://gizisikecil-app.onrender.com`
2. **Premium Page:** `https://gizisikecil-app.onrender.com/premium`
3. **Health Check:** `https://gizisikecil-app.onrender.com/health`
4. **API Docs:** `https://gizisikecil-app.onrender.com/docs`

**Test fitur:**
1. ✅ Klik "Coba Demo Data"
2. ✅ Klik "Analisis Sekarang"
3. ✅ Cek 5 grafik muncul
4. ✅ Download PDF & CSV
5. ✅ Test Tab Checklist

---

## 🔄 **UPDATE CODE (Jika Ada Perubahan)**

### Cara 1: Edit Langsung di GitHub Web

1. Buka repository di GitHub
2. Klik file `app.py`
3. Klik **icon pensil (Edit)**
4. Edit kode
5. Scroll bawah, commit changes
6. **Render akan AUTO RE-DEPLOY** dalam 2-3 menit!

### Cara 2: Upload File Baru

1. Hapus file lama:
   - Klik `app.py` → **icon titik tiga** → **Delete file** → Commit
2. Upload file baru:
   - **Add file** → **Upload files** → pilih `app.py` yang baru → Commit
3. Render auto re-deploy

---

## 🐛 **TROUBLESHOOTING**

### ❌ Error: "Build failed"

**Solusi:**
1. Cek **Logs** di Render dashboard
2. Biasanya masalah di `requirements.txt`
3. Pastikan ada baris: `git+https://github.com/natelandau/pygrowup.git@master`

### ❌ Error: "Application failed to start"

**Solusi:**
1. Cek Start Command: `uvicorn app:app --host 0.0.0.0 --port $PORT`
2. Pastikan `app.py` tidak ada syntax error
3. Cek Logs untuk detail error

### ❌ Error: "Connection timeout"

**Solusi:**
- Free tier Render sleep setelah 15 menit tidak diakses
- Buka URL lagi, tunggu 30-60 detik untuk "wake up"
- Atau upgrade ke Starter plan ($7/month) untuk always-on

### ❌ Repository tidak muncul di Render

**Solusi:**
1. Di Render, klik **Account Settings** (pojok kanan atas)
2. Pilih **Git Integrations**
3. Klik **Configure** di GitHub
4. Pastikan repository `gizisikecil` ter-centang
5. Kembali ke New Web Service dan refresh

---

## 📸 **SCREENSHOT PENTING (Untuk Referensi)**

Jika masih bingung, ini screenshot area penting:

### 1. GitHub - Upload Files
```
┌─────────────────────────────────────┐
│  < > Code   ▼ Add file   ...       │ ← Klik "Add file"
│      ├─ Create new file             │
│      └─ Upload files        ← INI!  │
└─────────────────────────────────────┘
```

### 2. Render - Create Web Service
```
┌───────────────────────────────────────┐
│ Build Command:                        │
│ ┌───────────────────────────────────┐ │
│ │ pip install -r requirements.txt   │ │ ← Copy exact
│ └───────────────────────────────────┘ │
│                                       │
│ Start Command:                        │
│ ┌───────────────────────────────────┐ │
│ │ uvicorn app:app --host 0.0.0.0    │ │ ← Copy exact
│ │ --port $PORT                      │ │
│ └───────────────────────────────────┘ │
└───────────────────────────────────────┘
```

---

## 🎊 **SELESAI!**

Sekarang aplikasi GiziSiKecil Anda sudah **LIVE** di internet! 🚀

**URL untuk share:**
```
https://[your-service-name].onrender.com
